{
  "tasks": {
    "dev": "deno run --watch --allow-net --allow-read --allow-write mod.ts",
    "start": "deno run --allow-net --allow-read --allow-write mod.ts",
    "build": "deno run --allow-read --allow-write --allow-net build.ts",
    "test": "deno test --allow-read --allow-write --allow-env --allow-sys",
    "test:watch": "deno test --watch --allow-read --allow-write --allow-env --allow-sys",
    "test:coverage": "deno test --coverage --allow-read --allow-write --allow-env --allow-sys",
    "fmt": "deno fmt",
    "lint": "deno lint",
    "check": "deno check **/*.ts",
    "compile": "deno compile --allow-net --allow-read --allow-write --output=quickpost mod.ts"
  },
  "compilerOptions": {
    "strict": true,
    "noImplicitAny": true,
    "strictNullChecks": true,
    "strictFunctionTypes": true,
    "strictBindCallApply": true,
    "strictPropertyInitialization": true,
    "noImplicitThis": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true
  },
  "lint": {
    "rules": {
      "tags": ["recommended"],
      "exclude": ["no-explicit-any"]
    }
  },
  "fmt": {
    "indentWidth": 2,
    "lineWidth": 100
  }
}
